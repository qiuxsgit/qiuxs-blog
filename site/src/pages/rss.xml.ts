import {loadBundle} from '../lib/bundle'; export async function GET(){const b=await loadBundle(); const items=b.articles.map(a=>`<item><title>${esc(a.title)}</title><link>https://qiuxs.com/posts/${a.slug}/</link><guid>https://qiuxs.com/posts/${a.slug}/</guid><description>${esc(a.summary)}</description><pubDate>${new Date(a.publishedAt).toUTCString()}</pubDate></item>`).join(''); return new Response(`<?xml version="1.0"?><rss version="2.0"><channel><title>${esc(b.site.name)}</title><link>https://qiuxs.com/</link>${items}</channel></rss>`,{headers:{'content-type':'application/xml'}})} const esc=(s:string)=>s.replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
