import {loadBundle} from '../lib/bundle'; export async function GET(){const b=await loadBundle(); const urls=['/','/posts/','/tags/','/archive/','/about/',...b.articles.map(a=>`/posts/${a.slug}/`),...b.tags.map(t=>`/tags/${t.slug}/`)]; return new Response(`<?xml version="1.0"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${urls.map(u=>`<url><loc>https://qiuxs.com${u}</loc></url>`).join('')}</urlset>`,{headers:{'content-type':'application/xml'}})}
